import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Coupons - Hi tech Copier Solutions | Digital Xerox Machine Discount Sales",
  description: "Canon Xerox Kyocera Discounts in Hyderabad amazing best prices coupon codes discount codes",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
